server:
  port: 8092
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Lilicheng123/config/
          username:  # git账号
          password:  # git密码
      label: master


